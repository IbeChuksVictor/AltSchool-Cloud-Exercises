---
- name: Install Apache2
  hosts: all
  become: yes
  pre_tasks:
    - name: Update apt cache
      apt:
        update_cache: yes
        upgrade: yes

  roles:
    - apache2
    - load_app
    - set_timezone  
